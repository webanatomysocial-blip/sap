<IfModule mod_rewrite.c>
  RewriteEngine On

  # Route all non-file/directory requests to index.php (API router)
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^ index.php [QSA,L]
</IfModule>
